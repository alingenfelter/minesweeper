const React = require('react')
const minesweeper = require('minesweeper')

const App = React.createClass({

  render() {
    return (
      <div>
        <h1>Welcome to Minseweeper</h1>
      </div>
    )
  }
})

module.exports = App
